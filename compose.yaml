services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env         
    volumes:
      - ./backend/processed_data:/app/backend/processed_data:ro
    restart: unless-stopped
